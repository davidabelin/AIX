runtime: python314
entrypoint: gunicorn -b :$PORT run:app

env_variables:
  # Hub-only service. Lab subpaths are dispatched to dedicated services.
  AIX_ENABLED_LABS: rps,drl,c4,clue,doubledigits,euclidyne,polyfolds
  AIX_DRL_APP_URL: https://drl-web-x2ulcmhaiq-wm.a.run.app
  RPS_DB_SNAPSHOT_URI: gs://aix-labs-data/rps/db/rps.sqlite3
  C4_DB_SNAPSHOT_URI: gs://aix-labs-data/c4/db/c4.sqlite3

service_account: aix-admin@aix-labs.iam.gserviceaccount.com
